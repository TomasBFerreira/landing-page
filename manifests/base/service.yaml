apiVersion: v1
kind: Service
metadata:
  name: databaes-landing-page
  namespace: databaes-landing-page
spec:
  type: LoadBalancer
  selector:
    app: databaes-landing-page
  ports:
    - port: 80
      targetPort: 80
