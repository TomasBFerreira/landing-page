apiVersion: v1
kind: Service
metadata:
  name: databaes-landing-page
  namespace: databaes-landing-page
spec:
  type: NodePort
  selector:
    app: databaes-landing-page
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
