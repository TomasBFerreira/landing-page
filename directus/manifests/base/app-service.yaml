apiVersion: v1
kind: Service
metadata:
  name: directus
  namespace: databaes-directus
spec:
  type: NodePort
  selector:
    app: directus
  ports:
    - port: 8055
      targetPort: 8055
      nodePort: 30055
